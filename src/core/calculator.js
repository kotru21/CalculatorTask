import { MemoryHandler } from './MemoryHandler';

/**
 * Основной класс калькулятора, объединяющий все функциональные модули
 * через механизм наследования
 */
export class Calculator extends MemoryHandler {
  // // Все необходимые методы уже доступны через цепочку наследования:
  // // Calculator -> MemoryHandler -> OperationsHandler -> EventHandler -> BaseCalculator
  // /**
  //  * Перегруженный конструктор для передачи информации родительским классам
  //  * @param {Display} display - объект отображения калькулятора
  //  */
  // constructor(display) {
  //   super(display);
  //   // Если в будущем потребуется дополнительная инициализация,
  //   // специфичная для этого класса, её можно добавить здесь
  // }
  // /**
  //  * Возможность добавления новых методов, специфичных только
  //  * для финального класса калькулятора
  //  */
}
